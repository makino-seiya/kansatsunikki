{
  "name": "plant-growth-tracker-frontend",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host 0.0.0.0 --port 3000",
    "dev:clean": "rm -rf .nuxt && rm -rf node_modules/.vite && npm run dev",
    "dev:docker": "nuxt dev --host 0.0.0.0 --port 3000 --watch-poll",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "^2.6.2",
    "nuxt": "^3.10.0"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.10.0",
    "axios": "^1.6.2",
    "chart.js": "^4.5.0",
    "vue-chartjs": "^5.2.0"
  }
}
